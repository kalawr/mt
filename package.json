{
  "name": "app",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "4.2.4"
  },
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd src && bower install && lodash include=debounce,throttle -o bower_components/lodash/custom/lodash.js && cd .."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bower": "^1.7.9",
    "cheerio": "^0.20.0",
    "entities": "^1.1.1",
    "express": "^4.13.4",
    "iconv-lite": "^0.4.13",
    "lodash-cli": "^4.11.1",
    "request": "^2.71.0"
  }
}
